# M1 voice pipeline — install in conda env hermes-agent:
#   pip install -r agent_platform/requirements-voice.txt

# D1–D2 (smoke)
edge-tts>=6.1.12
PyYAML>=6.0
numpy>=1.24
sounddevice>=0.4.6
scipy>=1.10
soundfile>=0.12
librosa>=0.10

# VAD (Silero; pulls torch)
silero-vad>=5.1
torch>=2.0
torchaudio>=2.0

# D3–D4
faster-whisper>=1.0.0
funasr>=1.1.0
dashscope>=1.20.0
modelscope>=1.15.0
openwakeword>=0.6.0
onnxruntime>=1.16.0
