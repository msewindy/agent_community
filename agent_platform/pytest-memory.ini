[pytest]
testpaths = tests
pythonpath = ..
addopts = -ra
markers =
    integration: requires MemVerse Docker or external services

# Memory + evolution local dev: run with
#   pytest -c pytest-memory.ini tests/test_audit.py ...
