# M6 tools / MCP (pydantic already via memory/proactive)
pydantic>=2.0
PyYAML>=6.0
# D3+ real MCP stdio (also bundled in hermes-agent venv)
mcp>=1.0
# D4 draft panel
fastapi>=0.110.0
python-multipart>=0.0.9
uvicorn>=0.27.0
httpx>=0.27.0
